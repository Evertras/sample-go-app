# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# IDE/OS stuff
.vim
.vscode
.DS_Store

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# We want to use vendor for some tools, but don't want to check it in
vendor/

# Built binaries
bin/

